# Frontend (Phase 1)

This is a minimal Vite + React + TypeScript scaffold used for Phase 1 of the `speckit-implement` flow.

Run locally:

```bash
cd frontend
npm install
npm run dev
```

Build:

```bash
npm run build
```
